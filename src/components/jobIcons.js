import { ArrowUp, Minus, TriangleAlert } from 'lucide-react';

/** Priority id → glyph, shared by the jobs table and the job detail heading. */
export const priorityIcons = {
  high: ArrowUp,
  normal: Minus,
  emergency: TriangleAlert,
};
